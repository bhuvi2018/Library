extends layout

block content
  h1= title
  
  ul
  each student in student_list
    li 
      a(href=student.url) #{student.name} 
      | (#{student.roll_no})

  else
    li There are no students.